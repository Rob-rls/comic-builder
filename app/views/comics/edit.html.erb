      <%= render "comic_form" %>
      <% if @strips.any? %>
        <% if @strip != nil %>
        <h2><%= @strip.name %></h2>
        Click on the background to edit the colour.
        <br />
        <div class="comic-strip clearfix color" style="background-color:<%= @comic.stripbackground%>;color:<%=@comic.stripcolor%>;" >
          <%= render "./strips/strip_view" %>
        </div>
          <br>
          <%= link_to "Edit Strip", edit_comic_strip_path(@comic, @strip) %>
          <% else %>
          <br>
          <%= link_to "Add Strip", new_comic_strip_path(@comic, @strip) %>
        <% end %>
      <br>
      <br>
      <% end %>
    </article>
  <div class="col-2">
    <br />
  </div>
</div>
